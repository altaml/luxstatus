# Core dependencies
pystray==0.19.4
pillow>=9.5.0
psutil>=5.9.0

# Luxafor device support (optional but recommended)
# Note: luxafor requires pyusb==1.0.0, so we use that version
pyusb==1.0.0
git+https://github.com/fmartingr/pyluxafor.git#egg=luxafor

# Platform-specific dependencies
pywin32>=304; sys_platform == "win32"
pyobjc-core>=9.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"

# Build tools (only install when needed)
# py2app>=0.28; sys_platform == "darwin"